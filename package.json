{
  "name": "portfolio-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Portfolio full-stack application monorepo",
  "workspaces": [
    "portfolio-api",
    "portfolio-ui",
    "shared"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "build:shared": "npm run build --workspace=shared",
    "build:api": "npm run build --workspace=portfolio-api",
    "build:ui": "npm run build --workspace=portfolio-ui",
    "dev:api": "npm run dev --workspace=portfolio-api",
    "dev:ui": "npm run dev --workspace=portfolio-ui",
    "watch:shared": "npm run watch --workspace=shared",
    "clean": "npm run clean --workspaces --if-present",
    "install:all": "npm install",
    "postinstall": "npm run build:shared",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "packageManager": "npm@10.9.0",
  "keywords": [
    "portfolio",
    "monorepo",
    "typescript",
    "full-stack"
  ],
  "author": "Portfolio",
  "license": "MIT",
  "devDependencies": {
    "@tailwindcss/container-queries": "^0.1.1"
  },
  "//overrides": "React runtime is 18.3.x. @testing-library/react allows @types/react ^18||^19 and npm hoisted 19 to this root, giving two React type identities and 6 bogus TS2786 'cannot be used as a JSX component' errors. Pin downward to 18.x. NOTE: frontend-ci.yml also runs a nested `npm install` inside portfolio-ui, where these root overrides do NOT apply — that case is covered independently because portfolio-ui/package.json declares @types/react ^18.3.23 itself. Keep both in step.",
  "overrides": {
    "@types/react": "^18.3.23",
    "@types/react-dom": "^18.3.7"
  }
}

name: Lighthouse CI

on:
  pull_request:
    paths:
      - 'portfolio-ui/**'
      - 'lighthouserc.json'
      - '.github/workflows/lighthouse-ci.yml'
  push:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: lighthouse-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lighthouse:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          # No `cache: npm` and `npm install` over `npm ci` — both need a
          # lockfile, and this repo intentionally commits none (see
          # portfolio-ui/Dockerfile, which uses npm install for the same
          # reason). setup-node hard-fails with the cache on and no lockfile
          # to key against; that is what killed this job from May 2026.

      - name: Install dependencies
        run: npm install --include=optional --no-audit --no-fund

      - name: Build UI
        run: npm run build:ui

      - name: Install Lighthouse CI
        run: npm install -g @lhci/cli@0.14.x

      - name: Run Lighthouse CI
        run: lhci autorun --config=./lighthouserc.json
        env:
          LHCI_GITHUB_APP_TOKEN: ${{ secrets.LHCI_GITHUB_APP_TOKEN }}

      - name: Upload Lighthouse reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lighthouse-reports
          path: .lighthouseci/
          retention-days: 14

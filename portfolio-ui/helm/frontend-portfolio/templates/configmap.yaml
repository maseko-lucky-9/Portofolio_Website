apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "frontend-portfolio.fullname" . }}
  labels:
    {{- include "frontend-portfolio.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}

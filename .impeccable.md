# Impeccable — Project Design Context

> This file is the source of truth for design decisions across the portfolio.
> All `/impeccable` invocations and any frontend work that touches aesthetics
> must read this section first. Update by re-running `/impeccable teach`.
>
> *Dual purpose: design context (Users / Brand Personality / Aesthetic
> Direction / Principles / Hard Rules) + design changelog (Verified
> Moves). The design-context sections take precedence when in conflict.*

## Design Context

### Users

**Primary audience.** Technical decision-makers evaluating Thulani for senior
contract work: engineering managers, platform leads, CTOs, and recruiters at
South African (and increasingly remote-EU/UK) banks, financial services, and
enterprise IT teams. They land here from LinkedIn, a referral, or a recruiter
shortlist. They are skimming on a laptop during work hours — not browsing for
delight, **auditing for credibility in under 60 seconds.**

**Secondary audience.** Peer engineers reading deep on architecture posts or
project write-ups, often after-hours, dark room, headphones on.

**Job to be done.** *"Can this person be trusted to own a regulated-industry
Kubernetes platform or .NET microservice without supervision?"* Every pixel
should answer yes. The site is a **trust artifact**, not a portfolio
playground.

**Context.** Desktop-first (recruiters and managers on work machines), with a
real mobile audience for LinkedIn click-throughs. Read in well-lit offices
during the day, dim rooms at night — both themes must be first-class.

### Brand Personality

**Three words.** *Composed. Operator. Specific.*

- **Composed** — quiet authority. Doesn't shout. Doesn't sell. The work does.
- **Operator** — someone who runs systems in production, not someone who
  reads about them. Numbers, infrastructure diagrams, run-book language.
- **Specific** — concrete: "MicroK8s + ArgoCD, GitOps, 99.95% over 14 months,"
  not "passionate full-stack engineer." Verbs over adjectives.

**Voice.** Plain, declarative, slightly under-stated. South African
engineering register — direct, dry, no marketing puffery, no emoji. Closer
to an SRE postmortem than a startup landing page.

**Emotional goal.** The reader should feel **reassured**. *"This person is
expensive and worth it."*

### Aesthetic Direction

**Bold direction.** Refined editorial — *engineering field-notes meets
financial-section newspaper*. Think *The Financial Times* tech column or a
well-typeset Stripe-era engineering blog, not a SaaS landing page and
absolutely not a "creative dev" personal site.

**Themes.** Both. Light is the default for daytime recruiter viewing
(serious, paper-like, generous margins). Dark is the late-night deep-read
mode (warm-neutral dark, never the cyan-on-black AI palette).

**Anti-references** (what this must NOT look like):

- Aurora / blurred-blob / glassmorphism developer portfolios. Never bring
  WebGL hero scenes back.
- Cyan-purple-pink gradient hero text. Banned outright.
- "Creative coder" portfolios with cursors, scroll-jacking, and Three.js
  hero scenes. Wrong audience signal.
- Generic shadcn-default rounded cards with the same padding everywhere.
- Reflex-list fonts (Inter, DM Sans, Fraunces, Plex, Space Grotesk,
  Outfit, Plus Jakarta Sans). Use the font-selection procedure to look
  further.

**References for the right feel:**

- Editorial restraint of long-form engineering writing (Bryan Cantrill posts,
  Increment magazine layouts).
- The print-like hierarchy of a serious tech publication.
- Hardware-product manuals from the 70s/80s — confident, captioned, dense.

### Design Principles

These five principles override anything else when in conflict. If a design
choice doesn't ladder to one of these, it doesn't ship.

1. **Credibility over delight.** Every element earns its place by raising
   trust. Decoration that doesn't is removed. Animation that doesn't is
   removed. No motion-for-motion's-sake. Reduced-motion is the default
   feel, not the fallback.

2. **Specific over generic.** Real numbers, real client tier (where NDA
   allows), real architecture nouns. Replace "I build scalable systems"
   with "ran a 14-node MicroK8s cluster for an SA tier-1 bank's internal
   PaaS." Microcopy is technical, not aspirational.

3. **Typographic hierarchy carries the design.** One distinctive display
   face + one quiet body face do the heavy lifting. Neither may be Inter,
   DM Sans, Plus Jakarta, Space Grotesk, Outfit, Fraunces, Playfair, or
   any other reflex font. Pick something that reads as **a serious
   publication**, not a SaaS pitch deck. Modular scale, generous leading,
   capped measure (~70ch).

4. **Asymmetric, captioned, dense — not card-grids.** Borrow from print:
   wide gutters, sidenotes, captions under figures, varied column widths.
   Skills are not a 3×3 icon grid. Projects are not equal cards. The
   experience timeline already zigzags — extend that editorial logic
   everywhere else.

5. **Neutral-warm palette, single accent, used sparingly.** Paper-warm
   neutrals tinted toward a single brand hue (warm amber / desaturated
   ochre / oxidized brass — pick one in build, not in tokens copied from
   the current violet+aurora system). One accent, ≤10% visual weight,
   reserved for active state, the one number you want read first, and
   the inline link underline. No multi-color "skill category" tinting.

### Hard Rules (derived)

- **Fonts:** Type pair is **Source Serif 4 Variable** (display) +
  **Public Sans Variable** (body) + **JetBrains Mono** (narrow technical
  use only — code blocks, tabular numerics). Future passes must use the
  font-selection procedure if revisiting; never reach back to the
  reflex list.
- **Color:** OKLCH tokens. Neutrals tinted toward the brand hue.
  No pure `#000` / `#fff`. No gradient text. No cyan-on-dark.
- **Brand-accent surface area cap:** the accent hue may not paint
  display-sized text (h1 hero name, oversized numerics). The accent is
  reserved for: active state, link underline, the `Maseko` underline,
  the "Open to work" pill, the photo hairline ring, scroll progress,
  skill-toggle active. Total visual weight ≤10%.
- **Dark mode is Deep ink (hue 248, low chroma).** Dark `--background`
  is `oklch(0.14 0.012 248)` as of 2026-05-19 (revision 5). Cool but
  nearly neutral — never neon. Hue shifted from 250 → 248 and chroma
  lifted 0.008 → 0.012 to read less flat after the coral retirement;
  still under the 0.015 dark-neutral ceiling. Hard ceiling:
  **do not raise chroma above 0.015 on any dark-mode neutral** —
  that's the boundary separating Deep ink from the AI cyan-tinted
  dark trap.
- **Brand-accent chroma exception (revision 5, 2026-05-19).** The mint
  `--primary` token is the single allowed token above the 0.015 chroma
  ceiling. Hue 145° (terminal-green) for both modes; lightness flips
  with mode because cream paper and Deep ink need different shades to
  pass WCAG:
  - **Light mode:** `oklch(0.45 0.17 145)` — deep mint, ~6.2:1 on cream
    BG (AA Normal). White button labels read ~6.6:1 (AAA Large).
  - **Dark mode:** `oklch(0.82 0.17 145)` — bright mint, ~12:1 on Deep
    ink. Dark text on the fill reads ~12:1 (AAA).
  Rationale: the accent must read as a *signal*, not a neutral; chroma
  0.17 is the minimum that registers as colour at either lightness
  level. This exception applies only to `--primary`, `--ring`, and
  `--sidebar-primary` (which all alias the same per-mode value). Every
  other token in `index.css` stays under the ceiling. New tokens that
  want chromatic emphasis must compose from `--primary` rather than
  introducing another high-chroma exception.
- **`--secondary` is intentionally close to `--muted`** in OKLCH
  (single-accent design direction). Do not use `text-secondary` /
  `bg-secondary` as a chromatic signal — they read monochrome by
  design. If a component needs chromatic emphasis, use `text-primary`
  (the single mint accent) or introduce a new semantic token with
  explicit justification.
- **Layout:** No card-everything. Use rules, captions, sidenotes, and
  asymmetric columns. `gap` over margins. 4pt spacing scale with
  semantic names.
- **Motion:** Page-load orchestration only. No scroll-triggered fireworks.
  Honor `prefers-reduced-motion` as the *target* aesthetic, not the
  degraded one. Use `grid-template-rows: 0fr → 1fr` for height
  transitions; never animate `height: auto`.
- **Aurora background:** retired. `PaperBackground.tsx` (column rules +
  masthead hairline + folio mark) is the canonical hero backdrop.
  WebGL hero scenes do not return.
- **Glassmorphism:** retired. `.glass` and `.glass-card` are flat paper
  surfaces with hairline borders, no `backdrop-filter`. Future
  components must not reintroduce blur backdrops.
- **Decorative marks yield to functional widgets.** If two elements
  compete for the same corner, the functional/informational one wins;
  the decorative one moves.
- **All bans** in `~/.claude/skills/impeccable/SKILL.md` `<absolute_bans>`
  apply, in particular: no `border-left` accent stripes, no gradient text.

### Verified Moves

These changes shipped and are visually confirmed via screenshot audit.

**A. Foundation pass** *(commit `70fb956`, 2026-05-18 AM)*

- `<AuroraBackground />` → `<PaperBackground />` in `HeroSection.tsx`
- Removed `--gradient-hero` inline + two blur-blob ambient glows
- `.glass` / `.glass-card` rewritten flat + hairline
- `Navbar` scrolled state: no more `backdrop-filter: blur(20px) saturate(180%)`
- `ExperienceSection` cards: no more `bg-card/80 backdrop-blur-sm`
- `BlogSection` read-time chip: no more `backdrop-blur-md`
- Type pair installed: Source Serif 4 Variable + Public Sans Variable
  replace Inter

**B. Audit-remediation pass** *(branch `feat/audit-remediation-2026-05-18`,
commits `f676512..1f29f11`, 2026-05-18 PM)*

> 11 sequential phases on a dedicated branch. OKLCH atomic migration
> for every semantic-role token; warm-coal dark mode. Hero recast: solid
> foreground name + coral underline on `Maseko` only, single-element
> profile photo ring (no gradient), inline metric sentence replacing the
> three-card template. ExperienceSection eyebrows retyped sans;
> height-auto animation → grid-template-rows. Cross-section gradient +
> glow sweep (Footer, Projects, Logo, Blog, Contact, CodeDemo).
> SkillsSection mono-tone (tri-color taxonomy retired). Navbar mobile
> menu blur removed; `App.css` deleted. `three.js` + Inter +
> `AuroraBackground/Canvas` + `useShouldRenderWebGL` deleted entirely.
> TS typing pass: typed `Transition` constants in `motion.ts`, Tag
> boundary casts. Token sweep: `--gradient-*`, `--shadow-glow*`,
> `--brand-violet*`, `--skill-*`, `--primary-glow` tokens removed from
> `:root` + Tailwind. Folio mark relocated to masthead anchor
> (top-right); theme-aware `--masthead-opacity`. P1 follow-up:
> availability pill contrast fixed (monochrome + coral pulse-dot).
> **Audit score 12/20 → 19/20.**

**C. Dark-mode repalette** *(2026-05-19)*

> Warm-coal `oklch(0.18 0.012 50)` → Deep ink `oklch(0.13 0.008 250)`.
> All dark-mode neutrals migrated to hue 250 with chroma 0.005–0.012;
> primary coral (hue 35) unchanged. Hue gap between background and
> accent moved from 15° to ~215° — muddy accent rendering resolved.
> Hard Rule on dark mode rewritten to record the override and lock the
> dark-neutral chroma ceiling at 0.015.

### Next Craft Passes (in suggested order)

Three outstanding items from the post-remediation `/audit` at 19/20:

1. **Container queries (`/adapt`).** Migrate `SkillsSection` chips and
   `ExperienceSection` cards to `@container` queries. Single move that
   takes Responsive 3/4 → 4/4 (and total 19/20 → 20/20).
2. **Recharts replacement (`/optimize`).** Replace the radar chart in
   `SkillsSection` with a hand-rolled SVG (~50 LOC for a single chart
   type). Reclaims ~98 KB gzip on the lazy chunk; allows deletion of
   the `charts-vendor` manualChunk + the `recharts` dep.
3. **Pre-existing TS hygiene (`/harden`).** 17 pre-audit TS errors in
   `mocks/`, `auth/`, `analytics.service`. Standalone pass; out of
   aesthetic scope.

*Mobile folio check (inline P3 reminder):* on `<640px` viewports the
folio mark in `PaperBackground.tsx` may collide with the theme-toggle
button at `top-6 right-6`. If observed, add `hidden sm:block` to the
folio div. No dedicated pass needed.

---

*Last updated 2026-05-19 (revision 4 — dark-mode repalette to Deep ink).*
*Stack: React 18 + Vite + Tailwind + shadcn/ui. Deployed to MicroK8s.*

---

## Revision 6 — Aura "Signal Field" (2026-09-06)

The site was rebuilt on the Aura Financial design direction, approved from a
full-page mockup before any repo change. This revision records what that
overrides, because most of it contradicts the Hard Rules above and a future pass
reading only that section would faithfully undo the redesign.

**Two things to know before reading the rules above.** First, they already
described a system the code had abandoned: revision 4 was written 2026-05-19,
and `4aaa2de` / `70fb956` reinstated Inter, indigo/emerald, gradients and
glassmorphism afterwards without amending this file. Second, that drift is now
resolved in the other direction — the rules below are what the code does.

| Rule above | Revision 6 | Why |
| --- | --- | --- |
| Type pair is Source Serif 4 + Public Sans | **Spectral (display, italic) + Public Sans Variable (body) + JetBrains Mono (labels)** | Chosen from three candidates rendered on the real hero. Self-hosted via `@fontsource`; the production CSP is `font-src 'self' data:`, so a Google Fonts stylesheet would be blocked and fall back silently. |
| No cyan-on-dark | **Cyan `#38BDF8` is the single accent**, as `--signal` | The one high-chroma exception, structurally parallel to the mint exception it replaces. Named `--signal` because shadcn's own `--accent` is a hover surface and both are valid OKLCH triples — a collision would have repainted every dropdown cyan with no error. |
| Both themes first-class | **Dark only** | Light mode, `ThemeContext` and the toggle are deleted. The app already defaulted to dark. |
| Never bring WebGL hero scenes back | **One ambient field, behind bail-outs** | It is the design's defining layer. It is also the rule most worth keeping half of: the field never blocks content, is loaded at idle after `load`, and every failure path — no WebGL2, opt-out flags, no scene, lost context — lands on a CSS gradient that ships in the first paint. |
| Glassmorphism retired; no `backdrop-filter` | **Allowed, for the section rhythm only** | Sections alternate translucency over the field instead of separating by gap. Not decoration: it is what makes the background legible as one continuous layer. |
| Modular scale | **Stepped px ramp, no `clamp()`** | 10·12·14·16·18·20·24·30·36·48·60·72. The 36→48 gap is deliberate — a body world and a display world with nothing between. |
| Accent may not paint display-sized text | **One exception: the hero's `.lit` line** | Three lines, one of them accent. The cap on total accent surface area still holds. |
| No pure `#fff` | **Ink is `#fff`, alpha-graded** | One ink at 1.0…0.2. `≤0.4` is decorative only, never text: on `--surface-page`, ink-50 measures 5.29:1 and ink-40 3.70:1. |
| 4pt spacing scale | Unchanged in spirit; the token names are Aura's | — |

**Still honoured, and worth stating because it constrained the port:** every
dark neutral sits at chroma ≤0.012, under the 0.015 ceiling that separates a
tinted dark from the cyan-tinted AI-dark trap. Only `--signal` exceeds it.

**Also unchanged:** motion is page-load orchestration plus the ambient field;
`prefers-reduced-motion` is a first-class target, not a degraded one — the field
holds a rendered frame rather than disappearing. Card-everything is still
avoided: the FAQ is rules and disclosure rows, Experience is a timeline, and
Services carries no tier ladder because there are no prices to rank.

*Revision 6 — 2026-09-06. Stack: React 19 + Vite 7 + Tailwind 3.4 + shadcn/ui.
Production is Cloudflare Workers; the homelab nginx image is the dev target.*

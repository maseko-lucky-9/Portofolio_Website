# Pre-commit hooks. Install once per clone:  pre-commit install
# Content-based secret scanning on every commit (filename-independent), honoring .gitleaks.toml.
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
